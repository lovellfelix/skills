# GrenadianBuzz API Compatibility Update

Date: 2026-03-20

`grenadianbuzz-api` is now a compatibility shim.

Canonical package: `skills/portable/grenadianbuzz/`

What changed:
- Deep reference materials were consolidated into `skills/portable/grenadianbuzz/`.
- This package keeps a minimal `SKILL.md` and `manifest.json` for backward compatibility.
- Personal-only activation behavior is preserved.

If you are starting new work, use `grenadianbuzz`.
